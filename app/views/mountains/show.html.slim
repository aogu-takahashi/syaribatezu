p = @mountain.name

= link_to "山情報を編集する", edit_mountain_path(@mountain)
= button_to "山を削除する", mountain_path(@mountain), method: :delete

- @mountain.prefectures.each do |prefecture|
  li = prefecture.name

= link_to "コースを追加する", new_mountain_course_path(@mountain.id)

= render "courses/courses", { courses: @courses }

= link_to "山一覧に戻る", mountains_path